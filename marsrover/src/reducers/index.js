const initialState = {
  position1: [],
  position2: []
};

const roverStates = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default roverStates;
